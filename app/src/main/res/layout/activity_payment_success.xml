<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/white"
    android:padding="24dp"
    android:gravity="center">

    <!-- Success Icon -->
    <ImageView
        android:layout_width="200dp"
        android:layout_height="200dp"
        android:src="@drawable/success_payment_icon"
        android:layout_marginBottom="40dp" />

    <!-- Success Title -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Selamat!"
        android:textSize="28sp"
        android:textStyle="bold"
        android:textColor="@color/black"
        android:layout_marginBottom="16dp" />

    <!-- Success Message -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Anda berhasil melakukan pembayaran,\nNikmati layanan kami!"
        android:textSize="16sp"
        android:textColor="@color/gray_text"
        android:gravity="center"
        android:lineSpacingExtra="4dp"
        android:layout_marginBottom="80dp" />

    <!-- Track Order Button -->
    <Button
        android:id="@+id/btnLacakPesanan"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:text="LACAK PESANAN"
        android:textColor="@color/white"
        android:textSize="16sp"
        android:textStyle="bold"
        android:background="@drawable/button_brown_background" />

</LinearLayout>